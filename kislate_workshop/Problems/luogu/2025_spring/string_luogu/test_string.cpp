#include <bits/stdc++.h>
using namespace std;
vector<bool> dp;
vector<string> myset;
int sum = 0;
int main(void)
{
	string temp;
	while(cin >> temp)
	{
		myset.push_back(temp}
		sum++;
	}
	string S;
	cin >> S;
	len = S.length();
	for(int i = 0; i < len; i++)
	{
		for(int j = 0; j < i; j++)
		{
			if(dp[j])
			{
				
			}
		}
	} 
}
