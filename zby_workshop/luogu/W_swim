#include<stdio.h>
int main() {
    double s, swim = 2.0, sum=0.0;
    int count;
    scanf("%lf", &s);
    for (count=0; sum < s; count++)
    {
        sum = sum + swim;
        swim = swim * 0.98;
    }
    printf("%d", count);
}